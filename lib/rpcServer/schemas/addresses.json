{
  "type": "object",
  "properties": {
    "address": {
      "type": ["array", "string"]
    },
    "from": {
      "type": ["integer"],
      "minimum": 0
    },
    "to": {
      "type": ["integer"],
      "minimum": 0
    }
  },
  "required": [
    "address"
  ],
  "if": { "properties": { "to": { "minimum": 0 } } },
  "then": { "required": [ "from" ] }
}
